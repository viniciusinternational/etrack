/**
 * PDF font setup. We use built-in Helvetica/Helvetica-Bold (see theme.ts)
 * so PDF generation never depends on external URLs or 404s.
 * To use custom fonts later, register them here and update theme.ts.
 */
// No Font.register() calls - built-in fonts only.
