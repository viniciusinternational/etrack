/**
 * Global loading page for E-Track
 * Shows while pages are loading
 */

import { LoadingPage } from '@/components/ui/loading';

export default function Loading() {
  return <LoadingPage message="Loading E-Track..." />;
}
